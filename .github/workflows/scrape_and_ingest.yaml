name: Scrape and ingest shows

on:
  schedule:
    # TEMP: Run every 15 minutes while verifying the scheduled scrape/ingest path.
    # Remove this after confirming the workflow succeeds on main.
    - cron: "*/15 * * * *"
    # Daily at 08:15 UTC (change as you like)
    - cron: "15 8 * * *"
  workflow_dispatch: {}

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r scripts/requirements.txt

      - name: Run ingestion
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: |
          python scripts/ingest/ingest_to_supabase.py
