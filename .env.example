# Supabase (Comediq2.0 — auth + database)
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Public browser token used only for Mapbox map rendering.
# Restrict this token in Mapbox to comediq.us and localhost.
VITE_MAPBOX_TOKEN=your_public_mapbox_token

SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Stripe Payment Link (create in Stripe Dashboard → Payment Links)
# Full Pass: $20/month
VITE_STRIPE_PAID_LINK=https://buy.stripe.com/your_paid_link

# Stripe Edge Function secrets — set via: supabase secrets set KEY=value
# For temporary local testing, put these in .env.local and run:
# supabase functions serve --env-file .env.local
# Then point the browser app at local functions:
# VITE_SUPABASE_FUNCTIONS_URL=
# STRIPE_SECRET_KEY=sk_live_...
# SNAPSHOT_STRIPE_WEBHOOK_SECRET=whsec_...
# STRIPE_PRICE_PAID=price_...
# SUPABASE_ANON_KEY=your_supabase_anon_key
# SITE_URL=https://comediq.us

# Admin Instagram comment collection dispatch
# Requires a GitHub token that can dispatch workflows in comediq-ig-api.
# GITHUB_IG_API_TOKEN=github_pat_...
# IG_API_GITHUB_REPOSITORY=malevcode/comediq-ig-api
# IG_API_REF=main
# IG_COMMENT_COLLECTION_WORKFLOW=collect_instagram_comments.yaml
# IG_MONTHLY_SEND_WORKFLOW=send_monthly_mic_verification.yaml
# IG_MONTHLY_COLLECT_WORKFLOW=collect_monthly_mic_verification.yaml
# The IG API GitHub workflows should read open_mics_historical from Supabase directly
# using repository/workflow secrets such as VITE_SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY.
