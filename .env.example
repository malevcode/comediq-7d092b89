# PocketBase backend URL — set this in Vercel → Settings → Environment Variables
# For local dev, the client falls back to http://127.0.0.1:8090 automatically
VITE_POCKETBASE_URL=https://comediq-pb.fly.dev
